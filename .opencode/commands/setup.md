---
description: Full backend setup
---

Run the complete backend setup:

```bash
cd api && composer setup
```

This installs dependencies, generates app key, runs migrations, and builds frontend assets.
